
version: '3.8'
services:
  lifeweaver:
    build: .
    image: lifeweaver-dev:latest
    volumes:
      - ./:/home/devuser/app
    ports:
      - "8000:8000"
      - "8888:8888"
    command: bash -c "python -m app.main"
